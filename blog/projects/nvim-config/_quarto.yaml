# NOTE: This configuration is important to the blog, ``output-dir`` should not
#       be configured so that builds work right in the blog.
project:
  type: website
  # output-dir: build # 
  preview:
    host: 0.0.0.0
    port: 3000
    browser: false
website:
  title: README preview
  site-url: https://acederberg.io/projects/nvim
  description: acederberg's blog.
  navbar:
    left:
      - href: index.qmd
        text: Home
format:
  html:
    theme: cosmo
